spring:
  application:
    name: driveindex
  main:
    allow-bean-definition-overriding: true
  web:
    resources:
      add-mappings: false
  datasource:
    driver-class-name: org.postgresql.Driver
vaadin:
  bun:
    enable: true